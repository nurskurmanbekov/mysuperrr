# 🔧 Исправление проблемы с MikroTik роутером

## Найденная проблема

Анализ конфигурации MikroTik показал:

```
❌ Порт 80 проброшен НА НЕПРАВИЛЬНЫЙ сервер:
   85.113.27.42:80 → 10.100.100.50:80 (китайский сервер)

✅ Backend работает на:
   192.168.88.24:8083
```

**Результат:** Запросы на `http://85.113.27.42/api/` попадают на китайский сервер, а не на Backend!

---

## Решение 1: Установить Nginx на 10.100.100.50 (РЕКОМЕНДУЕТСЯ)

Так как порт 80 уже проброшен на 10.100.100.50, просто установите там Nginx.

### Шаг 1: Подключитесь к серверу 10.100.100.50

```bash
ssh user@10.100.100.50
```

### Шаг 2: Установите Nginx

```bash
# Для Ubuntu/Debian:
sudo apt update
sudo apt install -y nginx

# Для CentOS/RHEL:
sudo yum install -y nginx
```

### Шаг 3: Примените конфигурацию

Скопируйте файл `nginx_for_10.100.100.50.conf` на сервер:

```bash
# С вашего компьютера:
scp nginx_for_10.100.100.50.conf user@10.100.100.50:/tmp/

# На сервере 10.100.100.50:
sudo cp /tmp/nginx_for_10.100.100.50.conf /etc/nginx/nginx.conf
```

### Шаг 4: Проверьте и запустите

```bash
# Проверка конфигурации:
sudo nginx -t

# Если OK - перезапустите:
sudo systemctl restart nginx
sudo systemctl enable nginx
```

### Шаг 5: Проверьте работу

```bash
# На сервере:
curl http://localhost/health
# Должно вернуть: OK - Nginx on 10.100.100.50 -> Backend 192.168.88.24:8083

# С внешнего мира (с телефона):
http://85.113.27.42/health
```

**ГОТОВО!** Мобильное приложение уже настроено на `http://85.113.27.42/api` ✅

---

## Решение 2: Изменить NAT на MikroTik

Если не можете установить Nginx на 10.100.100.50, измените правило NAT.

### Вариант А: Пробросить порт 80 напрямую на Backend

Подключитесь к MikroTik и выполните:

```mikrotik
# 1. Удалите старое правило проброса порта 80 на 10.100.100.50
/ip firewall nat
remove [find comment="Forward Port 80 to 10.100.100.50"]

# 2. Добавьте новое правило на 192.168.88.24:8083
/ip firewall nat
add action=dst-nat chain=dstnat comment="Forward Port 80 to Backend 192.168.88.24:8083" \
    dst-port=80 in-interface-list=WAN protocol=tcp \
    to-addresses=192.168.88.24 to-ports=8083

# 3. Проверьте
/ip firewall nat print where comment~"Forward Port 80"
```

**ВНИМАНИЕ:** При таком варианте API будет доступен как:
```
http://85.113.27.42:8083/api/
```

Нужно изменить в мобильном приложении:
```javascript
BASE_URL: 'http://85.113.27.42:8083/api'
```

### Вариант Б: Установить Nginx на 192.168.88.24

Если Backend на Windows (`192.168.88.24`), установите Nginx на той же машине:

1. **Следуйте инструкции** `SETUP_NGINX_WINDOWS.md`

2. **Измените NAT на MikroTik:**

```mikrotik
# Удалите старое правило
/ip firewall nat
remove [find comment="Forward Port 80 to 10.100.100.50"]

# Добавьте новое на 192.168.88.24:80
/ip firewall nat
add action=dst-nat chain=dstnat comment="Forward Port 80 to Nginx 192.168.88.24:80" \
    dst-port=80 in-interface-list=WAN protocol=tcp \
    to-addresses=192.168.88.24 to-ports=80
```

---

## Проверка после исправления

### 1. Проверка с сервера где Nginx:

```bash
# Nginx слушает порт 80?
sudo netstat -tulpn | grep :80

# Локально работает?
curl http://localhost/health
curl http://localhost/api/

# Backend доступен из сети?
curl http://192.168.88.24:8083/api/
```

### 2. Проверка с роутера MikroTik:

```mikrotik
# Проверьте NAT правила:
/ip firewall nat print where dst-port=80

# Должно показать правило на правильный сервер
```

### 3. Проверка извне (с телефона):

```
# Отключите WiFi, используйте мобильный интернет

http://85.113.27.42/health
# Должно вернуть: OK - ...

http://85.113.27.42/api/
# Должен вернуть ответ от Backend
```

---

## Текущая схема (НЕПРАВИЛЬНАЯ):

```
Телефон → 85.113.27.42:80
             ↓
          MikroTik
             ↓
       10.100.100.50:80 (китайский сервер)
             ↓
           ❌ 404
```

## Правильная схема (ПОСЛЕ ИСПРАВЛЕНИЯ):

```
Телефон → 85.113.27.42:80
             ↓
          MikroTik
             ↓
       10.100.100.50:80 (Nginx)
             ↓
       192.168.88.24:8083 (Backend)
             ↓
           ✅ Ответ API
```

---

## Какой вариант выбрать?

### ✅ Рекомендую: Решение 1

**Установите Nginx на 10.100.100.50**, потому что:
- Порт 80 уже проброшен туда
- Не нужно менять NAT правила
- Nginx отделён от Backend (лучше для безопасности)
- Мобильное приложение уже настроено правильно

### Если не можете:

**Решение 2 Вариант Б** - установите Nginx на той же машине где Backend (192.168.88.24)

---

## Что делать после установки?

1. ✅ Проверьте что всё работает
2. ✅ Протестируйте с мобильного приложения
3. ✅ Сделайте бэкап конфигурации MikroTik:
```mikrotik
/export file=backup-after-fix
```

---

## Нужна помощь?

Если есть вопросы или проблемы:
1. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`
2. Проверьте логи MikroTik: `/log print where topics~"firewall"`
3. Запустите скрипт диагностики: `./debug_nginx.sh`
