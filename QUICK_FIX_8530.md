# ⚡ Быстрое решение через порт 8530

## Ситуация

На MikroTik уже настроен проброс:
```
85.113.27.42:8530 → 192.168.88.24:80
```

НО! На сервере 192.168.88.24 не установлен Nginx на порту 80.

---

## Решение

### Шаг 1: Установите Nginx на 192.168.88.24

Подключитесь к серверу **192.168.88.24**:

```bash
# Если это Linux:
ssh user@192.168.88.24

# Установите Nginx:
sudo apt update
sudo apt install -y nginx
```

**Если это Windows** - используйте `SETUP_NGINX_WINDOWS.md`

### Шаг 2: Примените конфигурацию

```bash
# Скопируйте файл nginx_for_192.168.88.24.conf на сервер

# На Linux:
sudo cp nginx_for_192.168.88.24.conf /etc/nginx/nginx.conf

# На Windows:
# Скопируйте в C:\nginx\conf\nginx.conf
```

### Шаг 3: Запустите Nginx

**Linux:**
```bash
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl enable nginx
```

**Windows:**
```cmd
cd C:\nginx
nginx -t
start nginx
```

### Шаг 4: Проверьте

```bash
# На сервере 192.168.88.24:
curl http://localhost:80/health

# С внешнего мира:
curl http://85.113.27.42:8530/health
```

---

## Мобильное приложение

Уже обновлено! ✅

```javascript
// iosandroid/src/utils/constants.js
BASE_URL: 'http://85.113.27.42:8530/api'
```

---

## Схема работы

```
Телефон
  ↓
http://85.113.27.42:8530/api/
  ↓
MikroTik Router
  ↓
192.168.88.24:80 (Nginx)
  ↓
127.0.0.1:8083 (Backend на том же сервере)
```

---

## Проверка

### 1. Backend работает?
```bash
curl http://192.168.88.24:8083/api/
```

### 2. Nginx работает локально?
```bash
curl http://192.168.88.24:80/health
curl http://192.168.88.24:80/api/
```

### 3. Извне доступен через 8530?
```bash
curl http://85.113.27.42:8530/health
curl http://85.113.27.42:8530/api/
```

### 4. С телефона работает?
```
Открыть в браузере (через мобильный интернет, не WiFi):
http://85.113.27.42:8530/health
```

---

## Если Backend и Nginx на ОДНОМ сервере (192.168.88.24)

Отлично! Используйте конфигурацию `nginx_for_192.168.88.24.conf`:
- Nginx слушает порт 80
- Проксирует на **localhost:8083** (Backend на том же сервере)
- Доступ извне: **85.113.27.42:8530**

---

## ГОТОВО! 🎉

После выполнения всех шагов:
- ✅ MikroTik: порт 8530 проброшен правильно
- ✅ Nginx: установлен на 192.168.88.24:80
- ✅ Backend: работает на 192.168.88.24:8083
- ✅ Мобильное приложение: настроено на порт 8530

**Протестируйте с телефона!**
