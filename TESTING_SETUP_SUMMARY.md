# Сводка по настройке тестирования системы

## ✅ Выполненные задачи

### 1. Веб-приложение (React + TypeScript)
- ✅ Карта Кыргызстана настроена и оптимизирована
- ✅ Отображение осужденных на карте с маркерами
- ✅ Воспроизведение треков (Track Playback)
- ✅ Управление геозонами
- ✅ Панель администратора
- ✅ Реестр осужденных
- ✅ Исправлены ошибки консоли (бесконечные ре-рендеры)

### 2. Backend (Spring Boot + PostgreSQL)
- ✅ База данных настроена: `probationmob`
- ✅ Flyway миграции подготовлены
- ✅ Тестовый клиент добавлен в миграцию V10
- ✅ API эндпоинты готовы для мобильного приложения

### 3. Мобильное приложение (React Native + Expo)
- ✅ Создано руководство по тестированию: `MOBILE_TESTING_GUIDE.md`
- ✅ Настроена структура приложения
- ✅ API интеграция настроена
- ✅ Все экраны реализованы (Login, Home, Profile, FaceCheck, Camera)

---

## 🔑 Тестовые учетные данные

### Для мобильного приложения:
```
ИНН:     1234567890123
Пароль:  password123
```

### Для веб-панели (админ):
```
Email:    admin
Пароль:   admin
```

### База данных PostgreSQL:
```
Database: probationmob
User:     postgres
Password: 556055
Port:     5432
```

---

## 🚀 Как запустить систему

### 1. Запуск Backend

**Важно:** Для первого запуска требуется подключение к интернету (для загрузки Gradle зависимостей).

```bash
cd FreshBackend
./gradlew bootRun
```

**После успешного запуска вы увидите:**
```
Started ProbationBackendApplication in X.XXX seconds
Tomcat started on port(s): 8083 (http)
```

**Проверка работоспособности:**
```bash
curl http://localhost:8083/api/health
```

**Если возникла ошибка миграции Flyway:**
```bash
# Для Linux/macOS:
./fix-flyway.sh

# Для Windows:
fix-flyway.bat
```

---

### 2. Запуск веб-приложения

```bash
cd svezh
npm install  # Только при первом запуске
npm run dev
```

Откройте в браузере: **http://localhost:5173**

---

### 3. Тестирование мобильного приложения

Подробное руководство находится в **`MOBILE_TESTING_GUIDE.md`**

**Кратко:**

#### Вариант А: Expo Go (рекомендуется для быстрого тестирования)

1. Установите **Expo Go** на телефон:
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)

2. **ВАЖНО:** Измените API URL для локального тестирования:

   Откройте: `iosandroid/src/utils/constants.js`

   Найдите строку:
   ```javascript
   BASE_URL: 'http://85.113.27.42:80/api',
   ```

   Измените на:
   ```javascript
   // Для реального устройства (телефон и компьютер в одной Wi-Fi сети):
   BASE_URL: 'http://192.168.1.XXX:8083/api',  // Замените XXX на IP вашего компьютера

   // Для Android эмулятора:
   BASE_URL: 'http://10.0.2.2:8083/api',

   // Для iOS симулятора:
   BASE_URL: 'http://localhost:8083/api',
   ```

   **Как узнать IP компьютера:**
   - Windows: `ipconfig` → IPv4 Address
   - Linux/macOS: `ifconfig` → inet

3. Запустите dev сервер:
   ```bash
   cd iosandroid
   npm install  # Только при первом запуске
   npm start
   ```

4. Отсканируйте QR-код через Expo Go на телефоне

#### Вариант Б: Android эмулятор

```bash
cd iosandroid
npm run android
```

#### Вариант В: iOS симулятор (только macOS)

```bash
cd iosandroid
npm run ios
```

---

## 📡 API Эндпоинты

### Авторизация

**Login:**
```http
POST http://localhost:8083/api/auth/login
Content-Type: application/json

{
  "email": "1234567890123",
  "password": "password123"
}

Response:
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "inn": "1234567890123",
    "fio": "Иванов Иван Иванович",
    ...
  }
}
```

**Получить профиль:**
```http
GET http://localhost:8083/api/auth/me
Authorization: Bearer <token>

Response:
{
  "id": 1,
  "inn": "1234567890123",
  "fio": "Иванов Иван Иванович",
  "identifier": "test_client_001",
  "reg_address": "г. Бишкек, ул. Чуй 150",
  "fact_address": "г. Бишкек, ул. Чуй 150",
  "birth_date": "1990-01-15",
  "sex": "М",
  "passport": "AN1234567",
  "contact1": "+996555123456",
  "obs_start": "2024-01-01",
  "obs_end": "2025-12-31"
}
```

### Face-ID проверка

**Отправить селфи:**
```http
POST http://localhost:8083/api/facecheck/verify
Content-Type: multipart/form-data
Authorization: Bearer <token>

FormData:
  - userId: 1
  - file: <image file>

Response:
{
  "success": true,
  "distance": 0.35,
  "message": "Face verification successful"
}
```

### GPS позиция

**Отправить координаты:**
```http
POST http://localhost:8083/api/positions
Content-Type: application/json
Authorization: Bearer <token>

{
  "latitude": 42.8746,
  "longitude": 74.5698,
  "timestamp": "2024-11-18T10:30:00Z"
}
```

---

## 🧪 План тестирования мобильного приложения

### 1. Авторизация
- [ ] Вход с правильными данными (ИНН: `1234567890123`, пароль: `password123`)
- [ ] Вход с неправильными данными (должна появиться ошибка)
- [ ] Проверка сохранения сессии после закрытия приложения

### 2. Главный экран (Home)
- [ ] Отображается ФИО: "Иванов Иван Иванович"
- [ ] Статус последней проверки Face-ID
- [ ] Кнопки "Проверка Face-ID" и "Профиль" работают
- [ ] GPS статус отображается

### 3. Проверка Face-ID
- [ ] Запрос разрешения на камеру
- [ ] Камера открывается
- [ ] Можно сделать селфи
- [ ] Селфи отправляется на сервер
- [ ] Получен результат проверки (OK/FAILED)

### 4. Профиль
- [ ] Отображаются все данные осужденного:
  - ФИО: Иванов Иван Иванович
  - ИНН: 1234567890123
  - Дата рождения: 15.01.1990
  - Адрес: г. Бишкек, ул. Чуй 150
  - Телефон: +996555123456

### 5. GPS трекинг
- [ ] Запрос разрешения на геолокацию
- [ ] Координаты отправляются на сервер
- [ ] Работает в фоновом режиме

**Проверка на карте:**
- Откройте: http://localhost:5173/map
- Должна отображаться позиция осужденного на карте Кыргызстана

---

## ⚠️ Известные проблемы и решения

### 1. Backend не запускается (Flyway ошибка)

**Проблема:**
```
ERROR: Script V10__Add_Test_Client.sql failed
```

**Решение:**
```bash
./fix-flyway.sh  # Linux/macOS
fix-flyway.bat   # Windows
```

Подробнее: **`DATABASE_TROUBLESHOOTING.md`**

---

### 2. Мобильное приложение не подключается к backend

**Проблема:** Network Error

**Решения:**
1. Проверьте, что backend запущен: `curl http://localhost:8083/api/health`
2. Убедитесь, что BASE_URL в `iosandroid/src/utils/constants.js` правильный:
   - Для реального устройства: `http://192.168.1.XXX:8083/api` (ваш IP)
   - Для Android эмулятора: `http://10.0.2.2:8083/api`
   - Для iOS симулятора: `http://localhost:8083/api`
3. Телефон и компьютер должны быть в одной Wi-Fi сети
4. Временно отключите брандмауэр на компьютере

---

### 3. Карта не отображается в веб-приложении

**Проблема:** Белый экран на /map

**Решение:**
- Очистите кэш браузера (Ctrl+Shift+R / Cmd+Shift+R)
- Проверьте консоль браузера на ошибки (F12 → Console)

---

### 4. Камера не работает в мобильном приложении

**Проблема:** Camera permission denied

**Решение:**
1. Предоставьте разрешения в настройках телефона:
   - Android: Настройки → Приложения → Expo Go → Разрешения → Камера
   - iOS: Настройки → Expo Go → Камера
2. Пересоберите приложение: `npm start -- --clear`

---

## 📁 Структура проекта

```
my-super-project/
├── FreshBackend/                    # Spring Boot Backend
│   ├── src/main/resources/
│   │   ├── application.properties   # Конфигурация (порт 8083, БД)
│   │   └── db/migration/           # Flyway миграции
│   │       └── V10__Add_Test_Client.sql  # Тестовый клиент
│   └── build.gradle.kts
│
├── svezh/                           # React Frontend (веб-панель)
│   ├── src/
│   │   ├── components/
│   │   │   ├── map/RealMap.tsx     # Карта Кыргызстана
│   │   │   ├── playback/           # Воспроизведение треков
│   │   │   ├── geozones/           # Управление геозонами
│   │   │   └── ...
│   │   └── app.tsx                  # Главный компонент
│   └── package.json
│
├── iosandroid/                      # React Native Mobile App
│   ├── App.js                       # Точка входа
│   ├── src/
│   │   ├── screens/                 # Экраны (Login, Home, Profile, etc.)
│   │   ├── services/api.js          # API клиент
│   │   └── utils/constants.js       # Конфигурация (BASE_URL)
│   └── package.json
│
├── MOBILE_TESTING_GUIDE.md          # Подробное руководство по тестированию
├── DATABASE_TROUBLESHOOTING.md      # Решение проблем с БД
├── TESTING_SETUP_SUMMARY.md         # Этот файл
├── fix-flyway.sh / fix-flyway.bat   # Скрипты исправления Flyway
└── README.md
```

---

## 🎯 Следующие шаги

### Для локального тестирования:

1. **Запустите PostgreSQL** (если еще не запущен)
   ```bash
   # Linux:
   sudo systemctl start postgresql

   # macOS:
   brew services start postgresql

   # Windows:
   # Запустите через "Службы" или pgAdmin
   ```

2. **Запустите Backend:**
   ```bash
   cd FreshBackend
   ./gradlew bootRun
   ```

   Дождитесь сообщения: `Started ProbationBackendApplication`

3. **Запустите веб-приложение:**
   ```bash
   cd svezh
   npm run dev
   ```

   Откройте: http://localhost:5173

4. **Настройте и запустите мобильное приложение:**
   - Измените BASE_URL в `iosandroid/src/utils/constants.js`
   - Установите Expo Go на телефон
   - Запустите: `cd iosandroid && npm start`
   - Отсканируйте QR-код

5. **Тестируйте:**
   - Авторизуйтесь в мобильном приложении (ИНН: `1234567890123`, пароль: `password123`)
   - Сделайте Face-ID проверку (селфи)
   - Проверьте профиль
   - Включите GPS и подождите пару минут
   - Откройте карту в веб-приложении и проверьте, отображается ли позиция осужденного

---

## 📞 Справка

### Полезные команды

**Проверка состояния backend:**
```bash
curl http://localhost:8083/api/health
```

**Проверка БД:**
```bash
psql -U postgres -d probationmob
\dt  # Список таблиц
SELECT * FROM clients WHERE inn = '1234567890123';  # Проверка тестового клиента
\q   # Выход
```

**Очистка кэша Expo:**
```bash
cd iosandroid
npm start -- --clear
```

**Логи Android:**
```bash
adb logcat | grep ReactNativeJS
```

---

## ✅ Чек-лист готовности

- [x] Backend настроен (порт 8083)
- [x] База данных создана (probationmob)
- [x] Миграции подготовлены (включая тестового клиента)
- [x] Веб-приложение работает
- [x] Карта Кыргызстана настроена
- [x] Мобильное приложение настроено
- [x] API эндпоинты задокументированы
- [x] Тестовые учетные данные подготовлены
- [ ] **Backend запущен** (требует интернет для первого запуска)
- [ ] **Мобильное приложение протестировано**

---

**Готово к тестированию!** 🚀

Следуйте инструкциям выше и обращайтесь к **`MOBILE_TESTING_GUIDE.md`** для детального руководства по тестированию мобильного приложения.
