# 🏗️ АРХИТЕКТУРА СИСТЕМЫ МОНИТОРИНГА ОСУЖДЕННЫХ

## 📋 ОГЛАВЛЕНИЕ
1. [Обзор системы](#обзор-системы)
2. [Backend API](#backend-api)
3. [Мобильное приложение](#мобильное-приложение)
4. [Настройка MikroTik](#настройка-mikrotik)
5. [Тестирование](#тестирование)

---

## 🎯 ОБЗОР СИСТЕМЫ

### Компоненты:

```
┌─────────────────┐          ┌─────────────────┐          ┌─────────────────┐
│  Мобильное      │  HTTP    │   MikroTik      │  HTTP    │  Backend        │
│  приложение     │─────────>│   Firewall      │─────────>│  Spring Boot    │
│  (React Native) │  3728    │   (NAT/Filter)  │  8083    │  + Kotlin       │
└─────────────────┘          └─────────────────┘          └─────────────────┘
                                                                    │
                                                                    ▼
                                                           ┌─────────────────┐
                                                           │  PostgreSQL     │
                                                           │  probationmob   │
                                                           └─────────────────┘
```

### Технологии:

**Backend:**
- Spring Boot 3.2.0 + Kotlin 1.9.20
- PostgreSQL (база данных)
- JWT авторизация
- Traccar (GPS сервер на порту 8082)
- Firebase Cloud Messaging (FCM)
- JavaCV (распознавание лиц)

**Мобильное приложение:**
- React Native 0.81.5 + Expo 54
- Axios (HTTP клиент)
- AsyncStorage (локальное хранилище)
- Expo Camera, Location, ImagePicker

**Сеть:**
- MikroTik RouterOS (firewall + NAT)
- Внешний IP: `85.113.27.42`
- Внутренняя сеть: `192.168.88.0/24`

---

## 🔧 BACKEND API

### Базовая информация:

**Порт:** `8083` (внутренний)
**База:** PostgreSQL `probationmob` на `localhost:5432`
**Traccar:** `localhost:8082`
**JWT Secret:** `556055aaA!`

### Где работает backend:

| Адрес | Описание |
|-------|----------|
| `10.99.7.91:5000` | VM 1 |
| `10.99.7.95:5000` | VM 2 |
| `10.99.7.100:5000` | VM 3 |
| `192.168.88.24:8083` | Локальный (ваш компьютер) |

---

## 📡 API ENDPOINTS

### 1. Авторизация `/api/auth`

#### POST `/api/auth/login`
**Описание:** Вход в систему

**Request:**
```json
{
  "email": "1234567890123",
  "password": "password123"
}
```

**Response:**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "name": "1234567890123",
    "email": "1234567890123",
    "administrator": false,
    "attributes": {}
  }
}
```

#### GET `/api/auth/me`
**Описание:** Получить информацию о текущем пользователе

**Headers:**
```
Authorization: Bearer <JWT_TOKEN>
```

**Response:**
```json
{
  "user": {
    "id": 1,
    "name": "1234567890123",
    "email": "1234567890123",
    "administrator": false
  }
}
```

#### POST `/api/auth/logout`
**Описание:** Выход из системы (на клиенте удаляется токен)

---

### 2. Face-ID Проверка `/api/facecheck`

#### POST `/api/facecheck/verify`
**Описание:** Проверка лица (распознавание)

**Request:**
```
Content-Type: multipart/form-data

userId: 1234567890123
file: <binary image data>
```

**Response:**
```json
{
  "ok": true,
  "message": "Face verification successful",
  "distance": 0.35
}
```

**Алгоритм:**
1. Получает фото от мобилки
2. Сравнивает с эталоном из папки `./faces/`
3. Вычисляет distance (чем меньше, тем лучше совпадение)
4. Возвращает результат (ok: true/false)

#### POST `/api/facecheck/result`
**Описание:** Сохранение результата проверки Face-ID

**Request:**
```json
{
  "user_id": "1234567890123",
  "device_id": "test_client_001",
  "check_id": null,
  "outcome": "ok",
  "taken_at": 1700000000000,
  "distance": 0.35,
  "deadline_iso": "2024-11-19T12:00:00Z",
  "app_version": "1.0.0"
}
```

**Возможные значения outcome:**
- `ok` - успешно
- `failed` - не пройдено
- `declined` - отклонено
- `failed_network` - ошибка сети
- `late_ok` - успешно (поздно)
- `late_failed` - не пройдено (поздно)

**Response:**
```json
{
  "status": "received and saved"
}
```

#### POST `/api/facecheck/register-token`
**Описание:** Регистрация FCM токена для push-уведомлений

**Request:**
```json
{
  "device_unique": "1234567890123",
  "fcm_token": "dA1B2c3D4e5F6g7H8i9J0..."
}
```

**Response:**
```json
{
  "status": "success"
}
```

---

### 3. GPS Позиции `/api/positions`

#### POST `/api/positions`
**Описание:** Отправка GPS координат

**Request:**
```json
{
  "clientId": 1,
  "latitude": 42.8746,
  "longitude": 74.5698,
  "altitude": 800.0,
  "speed": 0.0,
  "accuracy": 10.0,
  "timestamp": "2024-11-19T10:30:00"
}
```

**Response:**
```json
{
  "id": 12345,
  "status": "saved"
}
```

#### GET `/api/positions/track/{clientId}`
**Описание:** Получение трека осужденного

**Parameters:**
- `clientId` - ID осужденного
- `startTime` - начало периода (ISO 8601)
- `endTime` - конец периода (ISO 8601)

**Example:**
```
GET /api/positions/track/1?startTime=2024-11-19T00:00:00&endTime=2024-11-19T23:59:59
```

**Response:**
```json
[
  {
    "id": 1,
    "latitude": 42.8746,
    "longitude": 74.5698,
    "altitude": 800.0,
    "speed": 0.0,
    "accuracy": 10.0,
    "timestamp": "2024-11-19T10:30:00"
  },
  ...
]
```

---

## 📱 МОБИЛЬНОЕ ПРИЛОЖЕНИЕ

### Структура API вызовов:

**Файл:** `iosandroid/src/services/api.js`

```javascript
import axios from 'axios';
import { API_CONFIG } from '../utils/constants';

const api = axios.create({
  baseURL: API_CONFIG.BASE_URL,  // http://85.113.27.42:3728/api
  timeout: 10000,
});

// Автоматическое добавление JWT токена
api.interceptors.request.use(async (config) => {
  const token = await AsyncStorage.getItem('authToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

### Используемые API:

**1. Авторизация:**
```javascript
authAPI.login('1234567890123', 'password123')
authAPI.getMe()
authAPI.logout()
```

**2. Face-ID:**
```javascript
faceCheckAPI.verifyFace(userId, photoFile)
faceCheckAPI.sendResult(resultData)
faceCheckAPI.registerFCMToken(deviceId, fcmToken)
```

**3. GPS:**
```javascript
traccarAPI.sendPosition(positionData)
```

---

## 🔥 НАСТРОЙКА MIKROTIK - ПОРТ 3728

### Схема проброса:

```
Интернет → 85.113.27.42:3728 → MikroTik NAT → 192.168.88.24:8083 → Backend
```

### Команды MikroTik:

```routeros
# ============================================
# 1. NAT ПРАВИЛО - Проброс порта 3728
# ============================================
/ip firewall nat add \
  action=dst-nat \
  chain=dstnat \
  comment="WAN:3728 -> Backend API 192.168.88.24:8083 (KG only)" \
  dst-port=3728 \
  in-interface-list=WAN \
  protocol=tcp \
  src-address-list=KG \
  to-addresses=192.168.88.24 \
  to-ports=8083

# ============================================
# 2. FIREWALL ACCEPT - Разрешить доступ из KG
# ============================================
/ip firewall filter add \
  action=accept \
  chain=forward \
  comment="Allow KG to Backend API port 3728" \
  dst-address=192.168.88.24 \
  dst-port=8083 \
  in-interface-list=WAN \
  protocol=tcp \
  src-address-list=KG \
  place-before=0

# ============================================
# 3. FIREWALL DROP - Блокировать не-KG
# ============================================
/ip firewall filter add \
  action=drop \
  chain=forward \
  comment="Block non-KG to Backend API 3728" \
  dst-address=192.168.88.24 \
  dst-port=8083 \
  in-interface-list=WAN \
  log=yes \
  log-prefix="BLOCK-3728: " \
  protocol=tcp \
  src-address-list=!KG \
  place-before=1
```

### Проверка правил:

```routeros
# Просмотр NAT правил
/ip firewall nat print where dst-port=3728

# Просмотр Firewall правил
/ip firewall filter print where dst-port=8083

# Проверка логов
/log print where message~"BLOCK-3728"
```

---

## ✅ ТЕСТИРОВАНИЕ

### 1. Запуск Backend

**На компьютере `192.168.88.24`:**

```bash
cd FreshBackend
./gradlew bootRun
```

**Проверка:**
```bash
curl http://localhost:8083/api/health
```

### 2. Запуск мобильного приложения

**На вашем компьютере (Windows):**

```powershell
cd iosandroid

# Обновить BASE_URL в src/utils/constants.js:
# BASE_URL: 'http://85.113.27.42:3728/api'

npm install
npx expo start --tunnel --clear
```

### 3. Тестовые данные

**Учетные данные:**
- ИНН: `1234567890123`
- Пароль: `password123`

**Тестовый клиент в БД:**
- ID: `1`
- ФИО: Иванов Иван Иванович
- Identifier: `test_client_001`
- Адрес: г. Бишкек, ул. Чуй 150

### 4. Сценарии тестирования

#### Сценарий 1: Авторизация

1. Открыть Expo Go на телефоне
2. Отсканировать QR-код
3. Ввести ИНН: `1234567890123`
4. Ввести пароль: `password123`
5. Нажать "Войти"

**Ожидаемое поведение:**
- Появляется главный экран
- Отображается ФИО: "Иванов Иван Иванович"

**API вызов:**
```
POST http://85.113.27.42:3728/api/auth/login
```

#### Сценарий 2: Face-ID проверка

1. На главном экране нажать "Проверка Face-ID"
2. Разрешить доступ к камере
3. Сделать селфи
4. Дождаться результата

**Ожидаемое поведение:**
- Фото отправляется на сервер
- Появляется результат (OK или FAILED)
- Результат сохраняется в БД

**API вызовы:**
```
POST http://85.113.27.42:3728/api/facecheck/verify (multipart/form-data)
POST http://85.113.27.42:3728/api/facecheck/result (JSON)
```

#### Сценарий 3: GPS трекинг

1. Разрешить доступ к геолокации
2. Приложение автоматически отправляет координаты каждые 30 секунд
3. Открыть веб-панель: `http://localhost:5173/map`
4. Проверить, что позиция отображается на карте

**Ожидаемое поведение:**
- Координаты отправляются в фоне
- Позиция видна на карте Кыргызстана

**API вызов:**
```
POST http://85.113.27.42:3728/api/positions
```

---

## 🔧 ДИАГНОСТИКА

### Проверка подключения к backend:

**С мобильного устройства:**
```bash
# Проверка доступности порта 3728
curl -v http://85.113.27.42:3728/api/health
```

**С локальной сети:**
```bash
# Проверка backend напрямую
curl http://192.168.88.24:8083/api/health
```

### Логи MikroTik:

```routeros
# Просмотр логов блокировок
/log print where message~"BLOCK-3728"

# Просмотр всех логов firewall
/log print where topics~"firewall"

# Живой просмотр логов
/log print follow where message~"3728"
```

### Логи Backend:

**Spring Boot:**
```bash
tail -f FreshBackend/logs/spring.log
```

**PostgreSQL:**
```bash
psql -U postgres -d probationmob

# Проверка последних проверок Face-ID
SELECT * FROM face_check_events ORDER BY taken_at DESC LIMIT 10;

# Проверка последних GPS позиций
SELECT * FROM positions ORDER BY timestamp DESC LIMIT 10;
```

---

## 📊 МОНИТОРИНГ

### Метрики для отслеживания:

1. **Backend:**
   - Uptime: `curl http://localhost:8083/actuator/health`
   - Количество запросов
   - Время ответа API

2. **База данных:**
   - Количество клиентов: `SELECT COUNT(*) FROM clients;`
   - Количество проверок: `SELECT COUNT(*) FROM face_check_events;`
   - Количество GPS позиций: `SELECT COUNT(*) FROM positions;`

3. **MikroTik:**
   - Количество запросов на порт 3728
   - Количество блокировок не-KG IP
   - Загрузка CPU/RAM

---

## 🚨 ЧАСТЫЕ ПРОБЛЕМЫ

### Проблема 1: Мобилка не подключается к backend

**Симптомы:**
- Network Error
- Timeout

**Решение:**
1. Проверить, что backend запущен: `curl http://192.168.88.24:8083/api/health`
2. Проверить правила MikroTik: `/ip firewall filter print where dst-port=8083`
3. Проверить, что ваш IP в списке KG: `/ip firewall address-list print where list=KG`
4. Временно отключить firewall: `/ip firewall filter disable [find where dst-port=8083]`

### Проблема 2: Face-ID не работает

**Симптомы:**
- Всегда FAILED
- Ошибка 500

**Решение:**
1. Проверить, что папка `./faces/` существует и содержит эталонные фото
2. Проверить права доступа: `chmod -R 755 ./faces/`
3. Проверить формат фото (JPG, PNG)
4. Проверить логи: `tail -f FreshBackend/logs/spring.log | grep FaceCheck`

### Проблема 3: GPS не отправляется

**Симптомы:**
- Координаты не появляются на карте
- Ошибка в логах

**Решение:**
1. Проверить разрешения Location в мобилке
2. Проверить, что clientId существует в БД: `SELECT * FROM clients WHERE id = 1;`
3. Проверить логи: `tail -f FreshBackend/logs/spring.log | grep Position`

---

## 📁 СТРУКТУРА ПРОЕКТА

```
my-super-project/
├── FreshBackend/                    # Spring Boot Backend
│   ├── src/main/Kotlin/
│   │   └── com/example/probationbackend/
│   │       ├── controller/          # API контроллеры
│   │       │   ├── AuthController.kt
│   │       │   ├── FaceCheckController.kt
│   │       │   └── PositionController.kt
│   │       ├── service/             # Бизнес-логика
│   │       ├── repository/          # JPA репозитории
│   │       └── model/               # Entity модели
│   ├── src/main/resources/
│   │   ├── application.properties   # Конфигурация (порт 8083)
│   │   └── db/migration/            # Flyway миграции
│   └── build.gradle.kts
│
├── svezh/                           # React Frontend (веб-панель)
│   ├── src/
│   │   ├── components/
│   │   │   ├── map/RealMap.tsx      # Карта Кыргызстана
│   │   │   ├── playback/            # Воспроизведение треков
│   │   │   └── geozones/            # Геозоны
│   │   └── app.tsx
│   └── package.json
│
├── iosandroid/                      # React Native Mobile App
│   ├── App.js
│   ├── src/
│   │   ├── screens/                 # Экраны
│   │   │   ├── LoginScreen.js
│   │   │   ├── HomeScreen.js
│   │   │   ├── FaceCheckScreen.js
│   │   │   └── ProfileScreen.js
│   │   ├── services/
│   │   │   └── api.js               # API клиент
│   │   └── utils/
│   │       └── constants.js         # BASE_URL конфигурация
│   └── package.json
│
├── SYSTEM_ARCHITECTURE.md           # Этот файл
├── MOBILE_TESTING_GUIDE.md          # Руководство по тестированию
└── TESTING_SETUP_SUMMARY.md         # Сводка настройки
```

---

## 🎯 ИТОГОВАЯ КОНФИГУРАЦИЯ

### Мобильное приложение:
```javascript
// iosandroid/src/utils/constants.js
BASE_URL: 'http://85.113.27.42:3728/api'
```

### MikroTik:
```
WAN:3728 → 192.168.88.24:8083
Firewall: KG разрешен, остальные заблокированы
```

### Backend:
```
Адрес: 192.168.88.24:8083
База: PostgreSQL probationmob:5432
Traccar: localhost:8082
```

### Тестовые данные:
```
ИНН: 1234567890123
Пароль: password123
```

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

**Backend логи:** `FreshBackend/logs/spring.log`
**База данных:** `psql -U postgres -d probationmob`
**MikroTik:** `ssh admin@85.113.27.42`

---

**Версия документа:** 1.0
**Дата:** 2024-11-19
**Автор:** Claude AI
