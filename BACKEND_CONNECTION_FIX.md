# 🔧 Исправление проблемы с подключением к Backend

## Проблема

Сервер **85.113.27.42** доступен, но возвращает **404 ошибку** вместо данных от API.

Из ваших логов видно:
- ✅ Spring Boot запустился успешно на порту **8083**
- ✅ Мобильное приложение настроено на **http://85.113.27.42/api**
- ❌ Nginx либо **не установлен**, либо **не настроен** на сервере 85.113.27.42

---

## Диагностика

Проблема: **Nginx не настроен или не запущен** на реальном сервере (85.113.27.42).

Текущая ситуация:
```
Мобильное приложение → 85.113.27.42/api/ → 404 (Not Found)
                                              ↑
                                   Nginx не проксирует на :8083
```

Должно быть:
```
Мобильное приложение → 85.113.27.42/api/ → Nginx → localhost:8083 → Spring Boot
                                              ↑
                                   Правильно настроен!
```

---

## Решение (Пошаговая инструкция)

### Шаг 1: Подключитесь к серверу 85.113.27.42

```bash
# Подключитесь по SSH к вашему серверу
ssh user@85.113.27.42

# Или как вы обычно подключаетесь к этому серверу
```

### Шаг 2: Скопируйте файлы на сервер

Нужно скопировать следующие файлы на сервер **85.113.27.42**:

```bash
# С вашего локального компьютера скопируйте на сервер:
scp /home/user/mysuperrr/nginx.conf user@85.113.27.42:/tmp/
scp /home/user/mysuperrr/debug_nginx.sh user@85.113.27.42:/tmp/
scp /home/user/mysuperrr/fix_nginx.sh user@85.113.27.42:/tmp/

# Или используйте любой другой способ передачи файлов (FTP, SCP, WinSCP и т.д.)
```

### Шаг 3: Запустите диагностику на сервере

```bash
# На сервере 85.113.27.42:
cd /tmp
chmod +x debug_nginx.sh
./debug_nginx.sh
```

Скрипт покажет детальную информацию о проблеме.

### Шаг 4: Примените автоматическое исправление

```bash
# На сервере 85.113.27.42:
cd /tmp
chmod +x fix_nginx.sh
sudo ./fix_nginx.sh
```

Этот скрипт автоматически:
1. Установит Nginx (если не установлен)
2. Создаст бэкап текущей конфигурации
3. Применит правильную конфигурацию из `nginx.conf`
4. Проверит синтаксис
5. Запустит Nginx
6. Проверит что всё работает

### Шаг 5: Убедитесь что Backend запущен на сервере

```bash
# Проверьте что Spring Boot работает на порту 8083:
netstat -tulpn | grep :8083

# Если порт НЕ слушается - запустите Backend:
cd /путь/к/FreshBackend
./gradlew bootRun

# Или как вы обычно запускаете ваш Backend
```

### Шаг 6: Проверьте что всё работает

```bash
# С сервера 85.113.27.42:
curl http://localhost/health
# Должно вернуть: OK - Backend:8083, Traccar:8082

curl http://85.113.27.42/health
# Должно вернуть тот же результат

curl http://localhost:8083/api/
# Проверка что Backend работает напрямую
```

### Шаг 7: Проверьте с мобильного приложения

Перезапустите мобильное приложение - оно уже настроено на правильный URL:
```javascript
BASE_URL: 'http://85.113.27.42/api'
```

---

## Альтернативное решение (если нет доступа к серверу)

Если у вас **НЕТ SSH доступа** к серверу 85.113.27.42:

### Вариант 1: Временно используйте прямое подключение

Обновите настройки в мобильном приложении:

```javascript
// iosandroid/src/utils/constants.js
export const API_CONFIG = {
  BASE_URL: 'http://85.113.27.42:8083/api',  // Прямой порт Backend
  TIMEOUT: 10000,
  RETRY_ATTEMPTS: 3,
  RETRY_DELAY: 1000,
};
```

⚠️ **Внимание:** Для этого порт **8083** должен быть открыт в firewall и проброшен на роутере!

### Вариант 2: Используйте ngrok для тестирования

```bash
# На машине где запущен Backend:
ngrok http 8083

# Используйте полученный URL в приложении
BASE_URL: 'https://xxxxx.ngrok.io/api'
```

---

## Проверка работы после исправления

### На сервере:

```bash
# 1. Nginx должен слушать порт 80
sudo netstat -tulpn | grep :80
# Должно показать: nginx ... :80

# 2. Backend должен слушать порт 8083
netstat -tulpn | grep :8083
# Должно показать: java ... :8083

# 3. Health check должен отвечать
curl http://localhost/health
# OK - Backend:8083, Traccar:8082

# 4. API должен быть доступен
curl http://localhost/api/
# Должен вернуть ответ от Spring Boot
```

### С внешнего устройства:

```bash
# С вашего компьютера или телефона:
curl http://85.113.27.42/health
# Должно вернуть: OK - Backend:8083, Traccar:8082
```

---

## Частые проблемы

### Проблема 1: Nginx не может подключиться к Backend

**Симптомы:** Nginx возвращает 502 Bad Gateway

**Причина:** Backend не запущен на порту 8083

**Решение:**
```bash
# Проверьте что Backend запущен
netstat -tulpn | grep :8083

# Если нет - запустите
cd /path/to/FreshBackend
./gradlew bootRun
```

### Проблема 2: Порт 80 занят другим процессом

**Симптомы:** Nginx не может запуститься

**Причина:** Apache или другой веб-сервер использует порт 80

**Решение:**
```bash
# Найдите процесс на порту 80
sudo netstat -tulpn | grep :80

# Остановите Apache (если он запущен)
sudo systemctl stop apache2
sudo systemctl disable apache2

# Запустите Nginx
sudo systemctl start nginx
```

### Проблема 3: Firewall блокирует порт 80

**Симптомы:** С сервера работает, с внешних устройств - нет

**Решение:**
```bash
# Откройте порт 80 в UFW
sudo ufw allow 80/tcp
sudo ufw status
```

### Проблема 4: Порт не проброшен на роутере

**Симптомы:** Localhost работает, внешний IP возвращает timeout

**Решение:** См. файл `PORT_FORWARDING_GUIDE.md` в репозитории

---

## Структура системы после исправления

```
┌─────────────────────────────────────────────┐
│  Мобильное приложение (React Native)        │
│  API: http://85.113.27.42/api              │
└──────────────────┬──────────────────────────┘
                   │
                   ↓ Интернет
┌─────────────────────────────────────────────┐
│  Сервер: 85.113.27.42                       │
│                                              │
│  ┌─────────────────────────────┐           │
│  │  Nginx :80                   │           │
│  │  ┌─────────────────────┐    │           │
│  │  │ /api/ → :8083       │    │           │
│  │  │ /traccar/ → :8082   │    │           │
│  │  │ /health → OK        │    │           │
│  │  └─────────────────────┘    │           │
│  └──────────┬──────────────────┘           │
│             │                                │
│  ┌──────────┴──────────┐                    │
│  │                     │                    │
│  ↓                     ↓                    │
│  Spring Boot :8083    Traccar :8082        │
│  (Backend API)        (GPS Tracking)        │
└─────────────────────────────────────────────┘
```

---

## Контрольный список

После выполнения всех шагов проверьте:

- [ ] Nginx установлен и запущен
- [ ] Конфигурация nginx.conf применена
- [ ] Backend запущен на порту 8083
- [ ] `curl http://localhost/health` возвращает OK
- [ ] `curl http://85.113.27.42/health` возвращает OK
- [ ] Мобильное приложение может подключиться к API
- [ ] GPS отслеживание работает

---

## Полезные команды

```bash
# Просмотр логов Nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# Перезагрузка Nginx без остановки
sudo systemctl reload nginx

# Проверка конфигурации Nginx
sudo nginx -t

# Просмотр статуса
sudo systemctl status nginx

# Проверка портов
sudo netstat -tulpn | grep -E ':80|:8083|:8082'
```

---

## Где находится сервер?

Важно понимать где физически находится сервер **85.113.27.42**:

1. **Если это VPS/VDS** (DigitalOcean, AWS, Hetzner и т.д.):
   - Используйте SSH для подключения
   - Применяйте скрипты напрямую на сервере

2. **Если это локальный сервер за роутером**:
   - Подключитесь к серверу локально или через SSH
   - Убедитесь что порт 80 проброшен на роутере (см. PORT_FORWARDING_GUIDE.md)

3. **Если это Windows машина**:
   - Возможно Backend запущен на Windows, но Nginx должен быть на Linux
   - Рассмотрите вариант с Docker или WSL

---

## Нужна помощь?

Если после всех шагов проблема не решена, соберите диагностическую информацию:

```bash
# На сервере 85.113.27.42:
./debug_nginx.sh > diagnostic_report.txt 2>&1
cat diagnostic_report.txt
```

И отправьте результат для анализа.
