# Server
server.port=8083
# =============================

# =============================

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
#
## Database
spring.datasource.url=jdbc:postgresql://localhost:5432/probationmob
spring.datasource.username=postgres
spring.datasource.password=556055
spring.datasource.driver-class-name=org.postgresql.Driver
#
## JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# ????????? ????????? security
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration,org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

# Flyway
flyway.enabled=true
flyway.locations=classpath:db/migration
flyway.url=jdbc:postgresql://localhost:5432/probationmob
logging.level.org.flywaydb=DEBUG

# Traccar Integration
traccar.base-url=http://localhost:8082
traccar.api.username=admin
traccar.api.password=admin

# FCM (Firebase Admin SDK)
# ???? ? ????? serviceAccountKey.json
# firebase.service-account-file-path=/path/to/serviceAccountKey.json

# Face Verification (???? ? ????? ? ?????????)
face.verification.known-faces-dir=./faces

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.com.example.probationbackend=DEBUG

# CORS (???? ????? ??????????)
# spring.web.cors.allowed-origins=http://127.0.0.1:5600
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# Firebase (???? ? ????? serviceAccountKey.json ? resources)
firebase.service-account-file-path=serviceAccountKey.json

# ???? ??? ???????? ????
app.photos.storage-dir=./photos
app.jwt.secret=556055aaA!
app.jwt.expiration=86400000


