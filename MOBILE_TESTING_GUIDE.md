# Инструкция по тестированию мобильного приложения

## 📱 О приложении

**Технология:** React Native + Expo
**Версия:** 1.0.0
**Функции:**
- Авторизация осужденных по ИНН и паролю
- Проверка Face-ID через камеру
- Отслеживание GPS местоположения
- Профиль осужденного
- Push-уведомления

---

## 🚀 Быстрый старт

### 1. Настройка Backend URL

**Для локального тестирования** нужно изменить адрес API в конфигурации.

Откройте файл: `iosandroid/src/utils/constants.js`

**Текущий адрес:**
```javascript
BASE_URL: 'http://85.113.27.42:80/api'
```

**Измените на локальный:**

**Вариант A: На реальном устройстве (Android/iOS)**
```javascript
BASE_URL: 'http://192.168.1.XXX:8083/api'  // Замените на IP вашего компьютера
```

**Вариант B: На эмуляторе Android**
```javascript
BASE_URL: 'http://10.0.2.2:8083/api'  // 10.0.2.2 = localhost для Android эмулятора
```

**Вариант C: На iOS симуляторе**
```javascript
BASE_URL: 'http://localhost:8083/api'
```

> 💡 **Как узнать IP вашего компьютера:**
> - Windows: `ipconfig` → IPv4 Address
> - Linux/macOS: `ifconfig` → inet

---

### 2. Установка зависимостей

```bash
cd iosandroid
npm install
```

---

### 3. Запуск приложения

#### Вариант A: Expo Go (проще всего)

1. **Установите Expo Go** на телефон:
   - Android: [Google Play](https://play.google.com/store/apps/details?id=host.exp.exponent)
   - iOS: [App Store](https://apps.apple.com/app/expo-go/id982107779)

2. **Запустите dev сервер:**
   ```bash
   npm start
   ```

3. **Сканируйте QR-код:**
   - Откроется Metro Bundler с QR-кодом
   - Отсканируйте через Expo Go приложение
   - Приложение загрузится на телефон

#### Вариант B: Android эмулятор

1. **Установите Android Studio** с эмулятором

2. **Запустите эмулятор**

3. **Запустите приложение:**
   ```bash
   npm run android
   ```

#### Вариант C: iOS симулятор (только macOS)

1. **Установите Xcode**

2. **Запустите приложение:**
   ```bash
   npm run ios
   ```

---

## 🧪 Тестирование функций

### 1. Авторизация

**Тестовый пользователь:**
- ИНН: `1234567890123`
- Пароль: `password123`

**Проверка:**
- ✅ Вход с правильными данными
- ✅ Ошибка с неправильными данными
- ✅ Сохранение сессии после закрытия

---

### 2. Главный экран (Home)

**Должно отображаться:**
- ФИО осужденного
- Статус последней проверки Face-ID
- Кнопка "Проверка Face-ID"
- Кнопка "Профиль"
- GPS статус (включен/выключен)

---

### 3. Проверка Face-ID

**Шаги:**
1. Нажмите "Проверка Face-ID"
2. Разрешите доступ к камере
3. Сделайте селфи
4. Приложение отправит фото на сервер
5. Получите результат проверки

**Проверка:**
- ✅ Запрос разрешения на камеру
- ✅ Предпросмотр фото
- ✅ Отправка на сервер
- ✅ Получение результата (OK/FAILED)

---

### 4. Профиль

**Должно отображаться:**
- ФИО
- ИНН
- Дата рождения
- Адрес регистрации
- Адрес фактический
- Телефон
- Тип надзора
- Даты начала/окончания надзора

---

### 5. GPS трекинг (фоновый режим)

**Проверка:**
- ✅ Запрос разрешения на геолокацию
- ✅ Отправка координат на сервер каждые N минут
- ✅ Работа в фоновом режиме

**Проверить на сервере:**
- Откройте веб-карту: http://localhost:5173/map
- Должна показываться позиция осужденного

---

## 🔧 Разработка и отладка

### Просмотр логов

**Expo CLI:**
```bash
npm start
# Нажмите 'j' для открытия Chrome DevTools
```

**React Native Debugger:**
```bash
# Установите
brew install --cask react-native-debugger  # macOS
# Или скачайте с GitHub для Windows/Linux

# Запустите на порту 19000
```

**Android logcat:**
```bash
adb logcat | grep ReactNativeJS
```

---

### Очистка кэша

Если приложение работает некорректно:

```bash
# Очистка Expo кэша
npm start -- --clear

# Очистка npm
rm -rf node_modules
npm install

# Очистка Android build
cd android
./gradlew clean
cd ..
```

---

## 📊 API Endpoints (используемые приложением)

### Авторизация
```
POST /api/auth/login
Body: { "email": "ИНН", "password": "пароль" }
Response: { "token": "...", "user": {...} }
```

### Получение профиля
```
GET /api/auth/me
Headers: Authorization: Bearer <token>
Response: { "id": 1, "fio": "...", ... }
```

### Проверка Face-ID
```
POST /api/facecheck/verify
Content-Type: multipart/form-data
Body: FormData { userId, file }
Response: { "success": true, "distance": 0.45 }
```

### Отправка GPS данных
```
POST /api/positions
Body: { "latitude": 42.87, "longitude": 74.57, "timestamp": "..." }
```

---

## ⚠️ Частые проблемы

### 1. Не подключается к backend

**Проблема:** Network Error
**Решение:**
- Проверьте, что backend запущен: `http://localhost:8083/api/health`
- Проверьте BASE_URL в `constants.js`
- Убедитесь, что телефон и компьютер в одной Wi-Fi сети
- Отключите брандмауэр на компьютере временно

---

### 2. Камера не работает

**Проблема:** Camera permission denied
**Решение:**
- Предоставьте разрешения в настройках телефона
- Пересоберите приложение: `npm start -- --clear`

---

### 3. GPS не отправляется

**Проблема:** Location permission denied
**Решение:**
- Предоставьте разрешения на геолокацию (всегда, а не только при использовании)
- Включите GPS на устройстве
- Проверьте, что Task Manager настроен корректно

---

### 4. Приложение вылетает

**Решение:**
```bash
# Перезапустите Metro Bundler
npm start -- --clear

# Проверьте логи
adb logcat | grep ReactNativeJS
```

---

## 📲 Сборка APK/IPA (Production)

### Android APK

```bash
# 1. Настройте подписание в android/app/build.gradle

# 2. Соберите APK
cd android
./gradlew assembleRelease

# APK будет здесь:
# android/app/build/outputs/apk/release/app-release.apk
```

### iOS IPA (только macOS)

```bash
# 1. Настройте сертификаты в Xcode

# 2. Соберите через Xcode или
expo build:ios
```

---

## 🧑‍💻 Структура проекта

```
iosandroid/
├── App.js                 # Точка входа
├── src/
│   ├── screens/          # Экраны приложения
│   │   ├── LoginScreen.js
│   │   ├── HomeScreen.js
│   │   ├── ProfileScreen.js
│   │   ├── FaceCheckScreen.js
│   │   └── CameraScreen.js
│   ├── services/         # API и сервисы
│   │   └── api.js
│   ├── store/           # Контекст и состояние
│   │   └── authContext.js
│   └── utils/           # Утилиты и константы
│       └── constants.js
├── android/             # Android нативный код
├── package.json
└── app.json            # Expo конфигурация
```

---

## 📝 Чеклист тестирования

### Функциональность
- [ ] Вход в систему (правильные данные)
- [ ] Вход в систему (неправильные данные)
- [ ] Проверка Face-ID (успешно)
- [ ] Проверка Face-ID (неуспешно)
- [ ] Просмотр профиля
- [ ] GPS трекинг в фоне
- [ ] Выход из системы

### UI/UX
- [ ] Загрузка экранов без лагов
- [ ] Корректное отображение на разных размерах экрана
- [ ] Навигация между экранами
- [ ] Кнопки реагируют на нажатия
- [ ] Индикаторы загрузки

### Интеграция
- [ ] Подключение к backend
- [ ] Отправка и получение данных
- [ ] Обработка ошибок сети
- [ ] Токен авторизации сохраняется
- [ ] Данные отображаются корректно

### Права доступа
- [ ] Разрешение на камеру
- [ ] Разрешение на геолокацию
- [ ] Разрешение на фоновую геолокацию

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `npm start` → нажмите 'j'
2. Проверьте backend: `http://localhost:8083/api/health`
3. Очистите кэш: `npm start -- --clear`
4. Переустановите зависимости: `rm -rf node_modules && npm install`

---

**Удачного тестирования! 📱🚀**
