# 🔌 Настройка проброса портов (Port Forwarding)

## Ваша конфигурация

- **Внешний IP:** 85.113.27.42
- **Внутренний IP сервера:** (нужно узнать - см. ниже)
- **Порты для проброса:** 80 (HTTP), 443 (HTTPS - опционально)

---

## Шаг 1: Узнайте внутренний IP вашего сервера

```bash
# Узнать IP сервера в локальной сети
ip addr show | grep "inet " | grep -v 127.0.0.1

# Или короче
hostname -I | awk '{print $1}'
```

Вы получите что-то вроде: `192.168.1.100` или `10.0.0.50`

**Запишите этот IP - он понадобится для настройки роутера!**

---

## Шаг 2: Настройка роутера

### 2.1 Вход в админку роутера

Откройте браузер и введите адрес вашего роутера (обычно один из):
- http://192.168.1.1
- http://192.168.0.1
- http://10.0.0.1
- http://router.local

Введите логин/пароль (обычно указан на наклейке роутера).

### 2.2 Найдите раздел Port Forwarding

Разные роутеры называют это по-разному:
- **Port Forwarding**
- **Virtual Server**
- **NAT Forwarding**
- **Переадресация портов**
- **Виртуальные серверы**

### 2.3 Добавьте правила

**Правило 1: HTTP (обязательно)**
```
Название: Probation-HTTP
Протокол: TCP
Внешний порт: 80
Внутренний IP: [IP вашего сервера из Шага 1]
Внутренний порт: 80
Включено: ✓
```

**Правило 2: HTTPS (опционально)**
```
Название: Probation-HTTPS
Протокол: TCP
Внешний порт: 443
Внутренний IP: [IP вашего сервера из Шага 1]
Внутренний порт: 443
Включено: ✓
```

### Примеры для популярных роутеров:

#### TP-Link
1. **Forwarding** → **Virtual Servers** → **Add New**
2. Service Port: 80, IP Address: [IP сервера], Internal Port: 80, Protocol: TCP
3. Нажмите **Save**

#### D-Link
1. **Advanced** → **Port Forwarding**
2. Name: Probation-HTTP, Public Port: 80, Private Port: 80, Private IP: [IP сервера]
3. Нажмите **Save Settings**

#### ASUS
1. **WAN** → **Virtual Server / Port Forwarding**
2. Enable: Yes, Service Name: Probation, Port Range: 80, Local IP: [IP сервера], Local Port: 80
3. Нажмите **Apply**

#### MikroTik
```bash
/ip firewall nat add chain=dstnat action=dst-nat \
  to-addresses=[IP сервера] to-ports=80 protocol=tcp dst-port=80
```

---

## Шаг 3: Настройка Firewall на сервере (Ubuntu/Debian)

```bash
# Проверяем статус ufw
sudo ufw status

# Если ufw не активен - активируем
sudo ufw enable

# Разрешаем SSH (ВАЖНО! Иначе потеряете доступ)
sudo ufw allow 22/tcp

# Разрешаем HTTP
sudo ufw allow 80/tcp

# Опционально: HTTPS
sudo ufw allow 443/tcp

# ВАЖНО: Закрываем прямой доступ к портам бэкенда
sudo ufw deny 8082/tcp
sudo ufw deny 8083/tcp

# Проверяем правила
sudo ufw status numbered
```

Должно быть примерно так:
```
Status: active

     To                         Action      From
     --                         ------      ----
[ 1] 22/tcp                     ALLOW IN    Anywhere
[ 2] 80/tcp                     ALLOW IN    Anywhere
[ 3] 443/tcp                    ALLOW IN    Anywhere
[ 4] 8082/tcp                   DENY IN     Anywhere
[ 5] 8083/tcp                   DENY IN     Anywhere
```

---

## Шаг 4: Проверка проброса портов

### 4.1 Проверка с самого сервера

```bash
# Проверяем что Nginx слушает порт 80
sudo netstat -tulpn | grep :80

# Должно быть:
# tcp  0  0  0.0.0.0:80  0.0.0.0:*  LISTEN  [PID]/nginx

# Проверяем что backend работает локально
curl http://localhost:8083/api/

# Проверяем что Traccar работает локально
curl http://localhost:8082

# Проверяем Nginx локально
curl http://localhost/health
curl http://localhost/api/
```

### 4.2 Проверка с внешнего адреса (с самого сервера)

```bash
# Health check
curl http://85.113.27.42/health

# API
curl http://85.113.27.42/api/

# Если возвращает ответ - отлично!
```

### 4.3 Проверка с другого устройства

С вашего компьютера или телефона (НЕ в той же сети):

```bash
# Из браузера откройте:
http://85.113.27.42/health

# Должны увидеть:
OK - Backend:8083, Traccar:8082

# Откройте Traccar UI:
http://85.113.27.42/traccar/
```

### 4.4 Онлайн проверка открытых портов

Используйте онлайн сервисы:

1. **CanYouSeeMe.org**
   - Откройте: https://canyouseeme.org/
   - Port: 80
   - IP: 85.113.27.42
   - Нажмите "Check Port"

2. **YouGetSignal**
   - Откройте: https://www.yougetsignal.com/tools/open-ports/
   - Remote Address: 85.113.27.42
   - Port Number: 80
   - Нажмите "Check"

3. **PortChecker.co**
   - Откройте: https://portchecker.co/
   - IP: 85.113.27.42
   - Port: 80

Если показывает **"Port is open"** или **"Success"** - всё работает! ✅

### 4.5 Через nmap (если установлен)

```bash
# С любого компьютера
nmap -p 80,443 85.113.27.42

# Вывод должен показать:
# 80/tcp  open  http
```

---

## Шаг 5: Проверка через мобильное приложение

```bash
# На телефоне в логах должны появиться:
🚀 Отправка GPS через Nginx → Spring Boot API
📡 Spring Boot ответ: 200 {...}
✅ GPS данные успешно отправлены
```

---

## 🚨 Troubleshooting

### Проблема: Порт закрыт при проверке онлайн

**Причины:**
1. ❌ Не настроен Port Forwarding на роутере
2. ❌ Firewall на сервере блокирует порт
3. ❌ Nginx не запущен
4. ❌ Провайдер блокирует порт 80

**Решение:**

```bash
# 1. Проверьте Nginx
sudo systemctl status nginx
sudo netstat -tulpn | grep :80

# 2. Проверьте firewall
sudo ufw status

# 3. Проверьте что порт доступен локально
curl http://localhost/health

# 4. Если локально работает, но извне нет - проблема в роутере или провайдере
```

### Проблема: Провайдер блокирует порт 80

Некоторые провайдеры блокируют порт 80 для "домашних" тарифов.

**Решение 1: Использовать другой порт**

Измените в `nginx.conf`:
```nginx
server {
    listen 8080;  # Было: 80
    ...
}
```

Пробросьте порт `8080` вместо `80`.

URL станут: `http://85.113.27.42:8080/api/`

**Решение 2: Использовать только HTTPS (443)**

Порт 443 обычно не блокируют. Настройте SSL и работайте через HTTPS.

---

## 📊 Схема работы

```
Телефон/Интернет
        ↓
   85.113.27.42:80 (Внешний IP)
        ↓
   [Роутер] Port Forwarding
        ↓
   192.168.x.x:80 (Внутренний IP сервера)
        ↓
   [UFW Firewall] - Разрешен порт 80
        ↓
   Nginx :80
        ↓
    ┌───┴───┐
    ↓       ↓
/api/    /traccar/
    ↓       ↓
:8083    :8082
(закрыты извне, доступны только через Nginx)
```

---

## ✅ Чек-лист

Используйте этот чек-лист для проверки:

- [ ] Узнал внутренний IP сервера
- [ ] Настроил Port Forwarding на роутере (порт 80)
- [ ] Настроил UFW на сервере (разрешил 80, закрыл 8082/8083)
- [ ] Nginx запущен и слушает порт 80
- [ ] `curl http://localhost/health` работает
- [ ] `curl http://85.113.27.42/health` работает
- [ ] Онлайн проверка показывает "Port 80 is open"
- [ ] Браузер открывает `http://85.113.27.42/traccar/`
- [ ] Мобильное приложение может подключиться к API

---

## 📞 Если ничего не помогло

1. **Проверьте логи Nginx:**
   ```bash
   sudo tail -f /var/log/nginx/access.log
   sudo tail -f /var/log/nginx/error.log
   ```

2. **Проверьте не использует ли порт 80 другая программа:**
   ```bash
   sudo netstat -tulpn | grep :80
   ```

3. **Временно отключите firewall для теста:**
   ```bash
   sudo ufw disable
   # Проверьте доступность
   # Затем обязательно включите обратно:
   sudo ufw enable
   ```

4. **Свяжитесь с провайдером** - возможно у них блокировка порта 80.

---

## 🔐 Рекомендации по безопасности

После того как все заработает:

1. **Настройте HTTPS** (см. NGINX_SETUP.md)
2. **Используйте fail2ban** для защиты от брутфорса
3. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
4. **Мониторьте логи** на подозрительную активность
5. **Настройте автоматический бэкап** базы данных

---

## 📚 Полезные ссылки

- Проверка портов: https://canyouseeme.org/
- Документация UFW: https://help.ubuntu.com/community/UFW
- Документация Nginx: https://nginx.org/ru/docs/
